{
  "type": "module",
  "devDependencies": {
    "@types/node": "20.0.0",
    "@vitest/coverage-v8": "^1.3.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf built node_modules coverage package-lock.json",
    "test": "vitest run test",
    "coverage": "vitest run test --coverage"
  }
}
